import type { DebugSnapshot } from "../../core/types/debug";

export const drawControlPanel = (
  _context: CanvasRenderingContext2D,
  _debugSnapshot: DebugSnapshot
): void => {
  // Phase 1 uses DOM controls only.
};
